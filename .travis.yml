language: python
python:
  - 2.7
before_install:
  - pip install coveralls
install:
  - pip install xmlrunner coverage pyyaml
script:
  - cd tests
  - coverage run tests.py
after_success:
    - coveralls